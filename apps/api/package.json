{
  "name": "@money-pilot/api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "d1:migrate:local": "wrangler d1 migrations apply money_pilot --local"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}

